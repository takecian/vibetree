# Backend (Express + Socket.IO). Used when running server or bin/npx.
VIBE_FLOW_PORT=3000

# Frontend (Vite) dev server port. Only used with `npm run dev`.
VITE_PORT=5173

# URL the client connects to for API and Socket. Must match VIBE_FLOW_PORT when dev.
VITE_API_URL=http://localhost:3000

# Optional: repo path and AI tool (can be set in UI instead).
# REPO_PATH=
# AI_TOOL=claude
